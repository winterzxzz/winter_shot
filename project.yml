name: WinterShot
options:
  bundleIdPrefix: com.winterzxzz
  deploymentTarget:
    macOS: "14.0"
  createIntermediateGroups: true

targets:
  WinterShot:
    type: application
    platform: macOS
    sources:
      - WinterShot
    info:
      path: WinterShot/Info.plist
      properties:
        CFBundleDisplayName: WinterShot
        CFBundleShortVersionString: "0.3.0"
        CFBundleVersion: "3"
        LSUIElement: true
        LSMinimumSystemVersion: "14.0"
        NSHumanReadableCopyright: "MIT License"
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.winterzxzz.WinterShot
        SWIFT_VERSION: "5.0"
        MACOSX_DEPLOYMENT_TARGET: "14.0"
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        # Sign with the local Apple Development cert of the personal team so
        # the code-signing requirement — and therefore the Screen Recording /
        # TCC grants — stays the same across rebuilds and reinstalls. Pinned
        # by team, not by cert hash, so a renewed cert keeps building. This
        # team matches the app currently installed in /Applications, keeping
        # its TCC grants; no cert for the former team H72HDR4743 exists in
        # this keychain anymore.
        CODE_SIGN_IDENTITY: "Apple Development"
        DEVELOPMENT_TEAM: 3YQ3747D5H
        CODE_SIGN_STYLE: Manual
        ENABLE_HARDENED_RUNTIME: NO
        COMBINE_HIDPI_IMAGES: YES
